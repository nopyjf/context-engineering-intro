# =============================================================================
# Multi-Agent System Configuration Template
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# LLM Provider Configuration
# -----------------------------------------------------------------------------
# Provider options: openai, anthropic, gemini
LLM_PROVIDER=openai

# Your LLM API Key
# OpenAI: Get from https://platform.openai.com/api-keys
# Anthropic: Get from https://console.anthropic.com/
# Gemini: Get from https://aistudio.google.com/app/apikey
LLM_API_KEY=your_llm_api_key_here

# Model to use
# OpenAI examples: gpt-4o-mini, gpt-4o, gpt-4-turbo
# Anthropic examples: claude-3-5-sonnet-20241022, claude-3-opus-20240229
# Gemini examples: gemini-1.5-pro, gemini-1.5-flash
LLM_MODEL=gpt-4o-mini

# API Base URL (usually default is fine)
LLM_BASE_URL=https://api.openai.com/v1

# -----------------------------------------------------------------------------
# Brave Search API Configuration
# -----------------------------------------------------------------------------
# Get your Brave Search API key from: https://brave.com/search/api/
# Free tier: 2000 requests per month
# Rate limits: Be mindful of monthly quota
BRAVE_API_KEY=your_brave_api_key_here

# Brave Search API endpoint (default is fine)
BRAVE_SEARCH_URL=https://api.search.brave.com/res/v1/web/search

# -----------------------------------------------------------------------------
# Gmail OAuth2 Configuration
# -----------------------------------------------------------------------------
# To get credentials.json:
# 1. Go to https://console.cloud.google.com
# 2. Create a new project or select existing
# 3. Enable Gmail API
# 4. Create OAuth 2.0 Client ID (Desktop app type)
# 5. Download credentials.json and place in ./credentials/ directory
#
# First run will open browser for authorization
# After authorization, token.json will be created automatically
GMAIL_CREDENTIALS_PATH=./credentials/credentials.json
GMAIL_TOKEN_PATH=./credentials/token.json

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Application environment
APP_ENV=development

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Enable debug mode for detailed logging
DEBUG=false
